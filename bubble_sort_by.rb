def bubble_sort_by (array)
    n = array.length
    loop do
        swap = false
        array.length do |left,right|
        if left.length <=> right.length 
            left = right
        swap = true
        end
        # left += 1
    end
    array
end
end
puts (["hi","hello","hey"])